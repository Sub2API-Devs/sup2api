{
  "version": "0.1.1-test",
  "description": {
    "en": "TEST BUILD of Guard with debug routes (POST /debug/dial, POST /debug/alloc). Do not use in production.",
    "zh": "请求守卫的测试构建，带调试接口（POST /debug/dial、POST /debug/alloc），请勿用于生产。"
  },
  "routes": [
    { "method": "GET", "path": "/rules", "scope": "admin", "permission": "rules:read" },
    { "method": "PUT", "path": "/rules", "scope": "admin", "permission": "rules:manage" },
    { "method": "GET", "path": "/stats", "scope": "admin", "permission": "stats:read" },
    { "method": "POST", "path": "/debug/dial", "scope": "admin", "permission": "rules:manage" },
    { "method": "POST", "path": "/debug/alloc", "scope": "admin", "permission": "rules:manage" }
  ]
}
