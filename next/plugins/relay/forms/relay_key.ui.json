{
  "ui:order": ["base_url", "api_key", "model_mapping"],
  "base_url": {
    "ui:widget": "url-presets",
    "ui:title": { "en": "Base URL", "zh": "Base URL" },
    "ui:placeholder": "https://relay.example.com",
    "ui:help": {
      "en": "Address of the Anthropic-compatible relay; requests go to <Base URL>/v1/messages. A trailing /v1 is removed.",
      "zh": "Anthropic 兼容中转站的地址，请求发往 <Base URL>/v1/messages；末尾的 /v1 会被去掉。"
    }
  },
  "api_key": {
    "ui:widget": "secret",
    "ui:title": { "en": "API key", "zh": "API Key" },
    "ui:placeholder": "sk-...",
    "ui:help": {
      "en": "Key issued by the relay, sent as x-api-key. Stored encrypted and never shown again.",
      "zh": "中转站发放的 Key，以 x-api-key 发送；加密保存，保存后不再明文显示。"
    }
  },
  "model_mapping": {
    "ui:widget": "model-mapping",
    "ui:title": { "en": "Model mapping", "zh": "模型映射" },
    "ui:help": {
      "en": "Optional: rewrite the requested model before forwarding, e.g. claude-opus-* -> claude-sonnet-5. Exact names win over wildcards.",
      "zh": "可选：转发前改写请求的模型，例如 claude-opus-* -> claude-sonnet-5。精确名称优先于通配符。"
    }
  }
}
