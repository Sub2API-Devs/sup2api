| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |# sub2api-next 开发契约（阶段 0 冻结；2026-09-24 并入阶段 1 交付的变更）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |本文件是所有开发 agent 的共同依据。**修改本文件、`sdk/proto`、`sdk/manifest`、`server/internal/core`、`server/internal/migrations/0001_core.sql` 必须经过主控**；发现契约不够用时，在交付说明里写明需要的改动，不要自行修改。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |设计背景见 [ARCHITECTURE.md](ARCHITECTURE.md)。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |---
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 1. 目录与负责人
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 路径 | 负责人 | 说明 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `sdk/proto`、`sdk/gen`、`sdk/manifest`、`sdk/protocol`（go-plugin 握手）、`sdk/pkgsig`（包签名格式） | 主控 | 契约 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/iam`、`authz`（含 `/me/menus`） | A1 identity | 用户、登录、JWT、step-up、RBAC、权限目录 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/apikey`、`group`、`proxy`、`account` | A2 resources | API Key、分组、代理、账号、账号类型接口 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/billing`（含 `billing/expr`）、`usage`、`event`（Emit 写入端） | B billing | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/plugin/pkg`（解包、manifest 校验、签名与信任）、`plugin/install`（上传、审查、授权确认、卸载）、`plugin/market`、`plugin/api`（`/plugins`、`/publishers`、`/market`、`/nodes`、`/ui/plugins` 接口） | C1 plugin-lifecycle | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/plugin/registry`（generation）、`plugin/grpcruntime`（进程、能力适配、HostService）、`plugin/rollout`（两阶段发布、对账）、`plugin/dbschema`（插件 schema、角色、迁移、DSN）、`plugin/routes`（`/api/v1/p/:key/*`、`/plugin-ui`） | C2 plugin-runtime | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/cluster`、`plugin/sandbox`、`plugin/egress`、`sdk/pluginsdk/egress` | D sandbox-network | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `sdk/pluginsdk`（除 `egress`）、`plugins/anthropic`、`plugins/guard`（Go 部分）、`tools/sub2api-plugin` | E sdk-plugins | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `web/`、`plugins/guard/ui/native` | F frontend | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/gateway`（含粘性会话、`/sticky-rules` 接口） | G gateway（阶段 2） | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `server/internal/event/delivery`、`server/internal/job` | H events-jobs（阶段 2） | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `deploy/`、`e2e/`、`Dockerfile` | QA | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |每个模块对外只暴露：构造函数、实现 `core` 接口的类型、`RegisterRoutes(r *httpapi.Router)`。**模块之间只通过 `core` 里的接口依赖，不 import 别人的包**（`core`、`store`、`httpapi`、`config`、`testutil` 除外）。组装由主控在 `internal/app` 完成。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 2. 构建与测试
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- Go 1.27；`next/go.work` 包含 `sdk`、`server`；新增的 Go module（插件、工具）加入 `go.work`，并在自己的 `go.mod` 里 `replace github.com/Sub2API-Devs/sup2api/next/sdk => ../../sdk`（按相对路径）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 下载依赖：`GOPROXY=https://goproxy.cn,direct`（只在命令里临时设置）；整理依赖用 `GOWORK=off go mod tidy`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 重新生成 proto：`cd sdk && buf generate`（工具在 `go env GOBIN`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 每个模块完成时必须通过：`go vet ./...`、`GOOS=linux go build ./...`、本模块的 `go test ./...`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- **数据库测试**：用 `testutil.DB(t)`，需要环境变量 `TEST_DATABASE_URL`（超级用户 DSN），没有时自动跳过。本机通过 SSH 隧道连接 ovh 上的测试库：`TEST_DATABASE_URL=postgres://postgres:sub2api@127.0.0.1:45432/postgres?sslmode=disable`（隧道：`ssh -N -L 45432:127.0.0.1:45432 -L 36379:127.0.0.1:36379 ovh`，主控已在本机常驻开启）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- **Redis 测试**：单元测试用 `github.com/alicebob/miniredis/v2`；集成测试可用 `TEST_REDIS_URL=redis://127.0.0.1:36379/0`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- Linux 专有代码用 `//go:build linux`，并提供非 Linux 的空实现，保证 Windows 上也能编译
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- **所有测试组件一律用 docker compose 启动，禁止在服务器上直接安装或运行任何服务/进程**。测试服务器 ovh 上：测试库为 compose 项目 `sub2api-next-testdb`（目录 `~/sub2api-next-test/testdb`）；需要在 Linux 上运行的 Go 测试（seccomp、/proc 等）用 `next/deploy/ci/compose.yml` 的 `gotest` 服务：把代码同步到 `~/sub2api-next-test/ci/<agent代号>/`，在该目录执行 `docker compose -f next/deploy/ci/compose.yml run --rm gotest go test ...`，用完删除同步目录。不要触碰服务器上的其他 compose 项目和容器
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 前端：Node 24，npm；`web/` 下 `npm ci && npm run build`，产物输出到 `server/web/dist`（由 `server/web` 用 `embed` 嵌入）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 3. 通用约定
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 3.1 REST 响应
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 情况 | 格式 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 成功 | `{"data": ...}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 列表 | `{"data": [...], "page": {"page": 1, "page_size": 20, "total": 123}}`，查询参数 `page`、`page_size`（最大 200） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 失败 | HTTP 状态码 + `{"error": {"code": "...", "message": "...", "details": {...}}}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 字段校验失败 | `invalid_argument`，`details.fields = [{"field","code","message"}]` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |错误码（`core/errors.go`）：`invalid_argument` 400、`unauthenticated` 401、`step_up_required` 403、`permission_denied` 403、`not_found` 404、`conflict` 409、`insufficient_balance` 402、`model_price_not_configured` 403、`model_not_allowed` 403、`rate_limited` 429、`no_available_account` 503、`plugin_unavailable` 503、`unavailable` 503、`internal` 500。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 3.2 数据格式
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- ID：JSON 数字（int64）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 时间：RFC 3339 字符串（UTC）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 金额：**字符串**，最多 8 位小数，如 `"12.34000000"`，Go 侧用 `decimal.Decimal`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 多语言文本：`{"en": "...", "zh": "..."}`；前端按当前语言取值，缺失时退回 `en`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- JSON 字段名：snake_case
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 3.3 鉴权
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 控制台：`Authorization: Bearer <access_token>`（JWT，HS256，`sub` 为用户 ID，默认 2 小时有效）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 敏感操作：先 `POST /api/v1/auth/step-up {"password"}` 得到 `step_up_token`（5 分钟有效，存 Redis），再在请求头带 `X-Step-Up-Token`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 路由注册：`r.Public` / `r.Authed` / `r.Perm(method, path, permission, handler)`；权限标记为 sensitive 时自动要求 step-up
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 网关：由插件声明的端点按 `auth.headers` 读取 API Key；错误格式按端点的 `errorFormat`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 4. 权限清单（核心）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |模块、key、是否敏感（🔐）。由 A 在 `authz` 中注册为 core 权限，启动时同步到 `permissions` 表。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 模块 | 权限 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || user | `user:read` `user:create` `user:update` `user:delete`🔐 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || role | `role:read` `role:manage`🔐 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || apikey | `apikey:self:manage` `apikey:all:read` `apikey:all:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || group | `group:read` `group:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || account | `account:read` `account:create` `account:update` `account:delete`🔐 `account:test` `account:credential:view`🔐 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || proxy | `proxy:read` `proxy:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || price | `price:read` `price:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || balance | `balance:self:read` `balance:all:read` `balance:adjust`🔐 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || usage | `usage:self:read` `usage:all:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || sticky | `sticky:read` `sticky:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || plugin | `plugin:read` `plugin:install`🔐 `plugin:manage` `plugin:uninstall`🔐 `plugin:grant:high` `plugin:grant:critical`🔐 `plugin:egress:read` `plugin:market:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || publisher | `publisher:read` `publisher:manage`🔐 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || node | `node:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || gateway | `gateway:use` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || settings | `settings:read` `settings:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |内置角色：`super_admin`（superuser）、`admin`（全部核心权限）、`user`（`apikey:self:manage` `balance:self:read` `usage:self:read` `gateway:use`）。新用户默认分配 `user` 角色。插件权限的 key 为 `plugin.<插件key>:<插件内 key>`，模块为 `plugin.<插件key>`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 5. REST API（`/api/v1`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |标注：负责人 / 所需权限（`-` 公开，`auth` 仅需登录）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.1 认证与当前用户（A）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 | 说明 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/auth/login` | - | `{email, password}` → `{access_token, refresh_token, expires_in, user}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/auth/refresh` | - | `{refresh_token}` → 同上（轮换 refresh token） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/auth/logout` | auth | 请求体 `{refresh_token?}` 可选；吊销该 refresh token |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/auth/step-up` | auth | `{password}` → `{step_up_token, expires_in}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me` | auth | `{id, email, display_name, roles:[key], permissions:[key], superuser}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me/menus` | auth | 侧边栏：`[{section, label:{en,zh}, items:[{id, label, icon, path, plugin_key?}]}]`；section 为 `overview/gateway/finance/system/me/plugins`（核心菜单按权限过滤，加上插件菜单） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || PUT `/me/password` | auth | `{old_password, new_password}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.2 用户、角色、权限（A）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/users`（`?q=&status=&role=`） | `user:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/users` `{email, display_name, password, role_keys[], max_concurrency}` | `user:create`；指定非默认角色另需 `role:manage` + step-up |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/PATCH `/users/:id` | `user:read` / `user:update` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || DELETE `/users/:id` | `user:delete` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || PUT `/users/:id/roles` `{role_keys[]}` | `role:manage`；只有超级管理员能授予/撤销 `super_admin` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/users/:id/groups`，PUT `/users/:id/groups` `{group_ids[]}` | `group:read` / `group:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/roles` / GET `/roles/:id` / POST `/roles` / PATCH `/roles/:id` / DELETE `/roles/:id` | `role:read` / `role:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || PUT `/roles/:id/permissions` `{permission_keys[]}` | `role:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/permissions` | `role:read`；按 module 分组：`[{module, label, source, plugin_key, status, permissions:[{key,label,sensitive,status}]}]` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.3 API Key、分组、代理（A）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/me/api-keys`，DELETE `/me/api-keys/:id` | `apikey:self:manage`；创建返回一次性明文 `key`（`sk-s2a-` 前缀） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me/groups` | auth（当前用户可用的分组） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/api-keys`，PATCH/DELETE `/api-keys/:id` | `apikey:all:read` / `apikey:all:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/groups`，GET/PATCH/DELETE `/groups/:id` | `group:read` / `group:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/proxies`，GET/PATCH/DELETE `/proxies/:id`，POST `/proxies/:id/test` | `proxy:read` / `proxy:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.4 账号（A；类型和表单来自插件注册表）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 | 说明 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/account-types` | `account:read` | `[{plugin_key, plugin_name, plugin_version, asset_base, platform, type, label, description, form:{mode, page?, component?}, sensitive_fields}]` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/account-types/:platform/:type/form` | `account:read` | `{schema, ui_schema}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/accounts`（`?platform=&group_id=&status=&q=`） | `account:read` | 列表含 `in_use`（实时并发）、`cooldown_until`、`orphaned` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/accounts` | `account:create` | `{name, platform, type, group_ids[], proxy_id, priority, max_concurrency, schedulable, credentials:{...}}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/PATCH `/accounts/:id` | `account:read` / `account:update` | 凭证中的敏感字段返回 `"******"`；PATCH 时敏感字段传 `"******"` 表示不修改 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || DELETE `/accounts/:id` | `account:delete` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/accounts/:id/test` | `account:test` | `{model?}` → `{ok, status, latency_ms, message}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/accounts/:id/credentials/reveal` | `account:credential:view` | 明文凭证 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.5 计费（B）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/prices`（`?platform=`），GET/PATCH/DELETE `/prices/:id`；详情返回 `analysis`（表达式读取的参数、请求头、指标） | `price:read` / `price:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/prices/validate` `{mode, config?, expression?, platform?}` → `{ok, expression, errors[], warnings[]}` | `price:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/prices/preview` `{price_id? \| mode+config/expression, usage:{p,c,cr,cc,cc1h,len?}, metrics:{}, headers:{}, params:{}, at?, group_id?}` → `{cost, base_cost, rate_multiplier, expression, expr_hash, tier, rules:[{cond,multiplier,matched}], breakdown:{...}}` | `price:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/prices/:id/override`（复制插件默认价格为管理员价格） | `price:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/prices/history/:expr_hash` | `price:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me/balance` → `{balance}`；GET `/me/ledger` | `balance:self:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/ledger`（`?user_id=&kind=`） | `balance:all:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/users/:id/balance/adjust` `{amount, credit:bool, note}`，支持请求头 `Idempotency-Key` | `balance:adjust` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me/usage`，GET `/me/usage/:id`，GET `/usage`（`?user_id=&group_id=&account_id=&model=&from=&to=&success=`），GET `/usage/:id` | `usage:self:read` / `usage:all:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/usage/summary`（`?from=&to=&group_by=day\|model\|user`） | `usage:all:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/PUT `/settings/billing` `{missing_price_policy: reject\|free, min_balance, big_cost_warning_usd}` | `settings:read` / `settings:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |价格表达式校验失败时 `details.fields[].message` 为 `{en, zh}`；表达式错误额外带 `detail`（位置信息）。结算时捕获的参数、请求头和 usage 口径存在 `usage_logs.billing_detail.inputs`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `/prices/preview` 的 `usage` 是按 exclusive 口径填写的 token 数（`p` 不含缓存），服务端按结算同样的规则归一化（ARCHITECTURE §7.3），所以试算结果与实际扣费一致
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 保存价格时，若表达式触发大额费用警告（`big_cost_warning_usd`），返回 400 `invalid_argument` 且 `details.confirmation_required=true`（附 `warnings`），前端二次确认后带 `confirm:true` 重新提交
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 列表接口 `/usage` 不含价格追溯字段；`price_id`、`expr_hash`、`billing_detail` 只在 `GET /usage/:id`、`/me/usage/:id` 返回
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 价格历史 `/prices/history/:expr_hash` 的 `expression` 为规范形式 `v1:<body>`；`/prices/:id` 保留录入时的文本，两者 `expr_hash` 相同
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.6 粘性会话（G 实现调度；规则 CRUD 由 G 负责）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/sticky-rules`，PATCH/DELETE `/sticky-rules/:id` | `sticky:read` / `sticky:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/sticky-rules/:id/flush` | `sticky:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/PUT `/settings/sticky` `{enabled, default_ttl_seconds, keep_on_account_disabled}` | `sticky:read` / `sticky:manage` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/sticky-rules/stats` → `[{rule, hits, misses, rebinds}]` | `sticky:read` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.7 插件（C；D 提供 egress 和资源数据）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 方法 路径 | 权限 | 说明 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins` | `plugin:read` | 列表：key、名称、状态、active/desired 版本、发布者、信任级别、各节点状态汇总 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key` | `plugin:read` | 详情：manifest 摘要、授权、节点状态、钩子统计、任务、事件游标、资源 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/upload`（multipart `file`） | `plugin:install` | → 安装审查信息 `review`（见下） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/install-from-market` `{source_id, key, version}` | `plugin:install` | → `review` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/versions/:version/review` | `plugin:read` | 重新获取待确认版本的 `review` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/:key/versions/:version/consent` `{grants:[{permission, scope?}], denied:[permission], role_keys_for_new_permissions:[]}` | `plugin:install` + 按风险 `plugin:grant:high`/`plugin:grant:critical` | 确认授权 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/:key/versions/:version/reject` | `plugin:install` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/:key/enable`、`/disable` | `plugin:manage` | 发起发布 → `rollout` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/plugins/:key/upgrade` `{version}` | `plugin:manage` | 发起升级 → `rollout` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/rollouts/current`，POST `/plugins/:key/rollouts/:id/cancel` | `plugin:read` / `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || DELETE `/plugins/:key?purge=true` | `plugin:uninstall` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/PUT `/plugins/:key/settings` | `plugin:read` / `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/grants`，DELETE `/plugins/:key/grants/:permission` | `plugin:read` / `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || PUT `/plugins/:key/resources` `{memory_mb, cpu, max_threads, max_open_files}` | `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || PUT `/plugins/:key/egress-policy` `{policy}` | `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/egress`（`?from=&to=`） | `plugin:egress:read` | 按域名汇总 + 明细 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/jobs`，POST `/plugins/:key/jobs/:job_id/run` | `plugin:read` / `plugin:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/plugins/:key/events` | `plugin:read` | 游标、积压、死信 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/ui/plugins` | auth | 前端扩展：`[{key, version, asset_base, menus, pages, slots, native_entry, trust}]`（按权限过滤） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/nodes` | `node:read` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/market/sources`、GET `/market/plugins?source_id=` | `plugin:market:read` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/publishers`，POST `/publishers/:id/keys`，POST `/publishers/:id/revoke`，POST `/publisher-keys/:key_id/revoke` | `publisher:read` / `publisher:manage` | |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |补充约定（C1）：
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 插件配置加密存于 `plugin_installs.config_enc`，AES-GCM 的 AAD 为 `"plugin-config:"+key`；配置变更后在 `config:changed` 广播 `{"type":"config","plugin_key":k}`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 宿主版本取 `main.Version`，比较兼容范围时忽略预发布后缀
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `/nodes` 与插件详情中每个节点的插件状态字段为 `state`（`pending|ready|active|failed`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 市场源 `url` 可以指向 `index.json`，也可以是以 `/` 结尾的目录（自动补 `index.json`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 插件详情中的钩子统计来自 `core.HookStatsSource`（G），手动执行任务通过 `core.JobTrigger`（H）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 发布行为（C2）：Disable 立即提交，返回时状态已是 `disabled`；Enable 优先启用 `active_version`，否则取最新的已批准版本；`plugins` 行删除后，各节点在一次对账内停止实例
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- **内置插件**（`plugins.builtin=true`，本期为 anthropic）：随镜像提供（`SUB2API_BUILTIN_PLUGIN_DIR`，默认 `/opt/sub2api/builtin`），核心启动时在一个节点上（锁 `plugins:builtin`）自动上传、授予全部宿主权限（新插件权限授予 `admin` 角色）、首次安装后启用，镜像带新版本时自动升级；管理员禁用后保持禁用。签名密钥由入口脚本通过 `SUB2API_BUILTIN_TRUST_KEY` 始终信任。`DELETE /plugins/:key` 对内置插件返回 403，`details.reason = "builtin"`；列表与详情返回 `builtin` 字段
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 升级包的宿主权限没有新增或扩大时，上传即沿用原授权（版本直接为 `approved`）；否则进入 `awaiting_consent`，旧版本继续运行
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 插件设置：GET `/plugins/:key/settings` → `{schema, ui_schema, values}`；PUT 请求体 `{values:{...}}`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `/ui/plugins` 每项另含 `host_ui_compat`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |`review` 结构：`{plugin_key, version, name, publisher, trust, signature_status, host_compat_ok, capabilities[], gateway_endpoints[], platform:{id, protocols}, account_types:[{id, label, protocols, form_mode}], hooks[], jobs[], events[], routes[], menus[], user_permissions[], database:{schema, migrations[]}, resources, external_services[], host_permissions:[{id, risk, scope, reason, optional, requires:"plugin:grant:high|critical"}], diff?:{added[], widened[], removed[]}}`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.8 插件自己的接口与静态资源（C）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `ANY /api/v1/p/:key/*path`：按 manifest `routes` 匹配；`scope=admin|user` 需要登录并检查 `plugin.<key>:<permission>`；`webhook` 和 `public` 不鉴权；转发给插件 `HTTPService.HandleHTTP`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `GET /plugin-ui/:key/:version_hash/*path`：插件包内 `ui/`、`forms/`、`i18n/` 下的文件；带长缓存头
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 5.9 网关端点（G）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |由 generation 中的 `EndpointBinding` 动态注册（`engine.NoRoute` 之前的分发中间件，参考 new-api `router/plugin-router.go`）。本期只有 `kind=proxy`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 6. 事件（outbox 表 `events`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |`core.EventPublisher.Emit(ctx, tx, events...)` 在业务事务里写入。payload（JSON）：
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 类型 | payload | 写入方 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `usage.recorded` | `{request_id, user_id, api_key_id, group_id, account_id, plugin_key, platform, protocol, model, success, status_code, error_type, input_tokens, output_tokens, cache_read_tokens, cache_creation_tokens, cache_creation_1h_tokens, total_cost, billing_status, latency_ms, created_at}` | B（结算后） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `account.created` / `account.updated` / `account.deleted` | `{account_id, plugin_key, platform, type, name}` | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `account.status_changed` | `{account_id, platform, status, reason, cooldown_until?}`；status 含 `cooldown` | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `user.created` / `user.updated` | `{user_id, email, status}` | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `balance.changed` | `{user_id, ledger_id, delta, balance_after, kind}` | B |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `plugin.enabled` / `plugin.disabled` | `{plugin_key, version}` | C |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 7. Redis key
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || key | 类型 | 负责人 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `node:live` | ZSET boot_id → 心跳毫秒 | D |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `node:info:{boot_id}`、`node:plugins:{boot_id}` | HASH，TTL 15s | D |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `slot:{kind}:{id}` | ZSET member=`{boot_id}:{request_id}` score=过期毫秒 | D |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `lock:{name}` | STRING owner token | D |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `cooldown:account:{id}` | STRING reason，TTL | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `stepup:{token}` | STRING user_id，TTL 5m | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `apikey:{sha256}` | STRING 缓存（JSON），TTL 60s | A |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `balance:{user_id}` | STRING 余额缓存 | B |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `sticky:{rule}:{group}:{model}:{hash}` | STRING account_id，TTL | G |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `sticky:stats:{rule}` | HASH hits/misses/rebinds | G |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `plugin:kv:{plugin_key}:{ns}:{key}` | STRING | C |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `hook:breaker:{plugin}:{hook}` | STRING 熔断截止毫秒时间戳，TTL 30s | G |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `hook:stats:{plugin}:{hook}` | HASH 调用/拒绝/超时/错误计数与延迟桶，TTL 7d | G |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `hook:statidx:{plugin}` | SET 该插件出现过的钩子 id，TTL 7d | G |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `plugin:ledger:{key}:{credit\|debit}:{yyyymmdd}` | STRING 插件当日入账/扣款累计，TTL 48h | C2 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |广播频道：`plugin:events`、`authz:changed`、`account:changed`、`config:changed`（`core/ports_cluster.go`）。权限版本以 PG `authz_meta` 为准，Redis 不存。`config:changed` payload：代理 `{"type":"proxy","id":N}`，插件配置 `{"type":"config","plugin_key":k}`。`plugin:events` payload：`{"type":"rollout"|"config","plugin_key","rollout_id"?}`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |节点插件状态（`node:plugins:{boot_id}` 每个插件一个 JSON，C2 写、C1 与控制台读）：`{state, serving, standby?, rollout_id?, rollout?, error?, instances:[{version, state, error?, restarts}]}`。`state` 汇总本节点：有进行中的发布时等于 `rollout`（`pending|ready|active|failed`），否则按在服务的实例为 `active|pending|failed`，没有在服务的版本为 `stopped`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |槽位回收（D）要求 Redis 为单实例（非 Cluster），且各节点时钟经 NTP 同步。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 8. 系统设置（`settings` 表）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || key | 值 | 默认 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `billing` | `{missing_price_policy, min_balance, big_cost_warning_usd}` | `reject`、`"0"`、`"10"` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `sticky` | `{enabled, default_ttl_seconds, keep_on_account_disabled}` | `true`、3600、`false` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || `gateway` | `{max_attempts, platform_call_timeout_ms, default_hook_timeout_ms}` | 3、2000、300 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 9. 环境变量
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |见 `server/internal/config/config.go`。测试额外使用 `TEST_DATABASE_URL`、`TEST_REDIS_URL`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 10. 交付要求（每个 agent）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |1. 只修改自己负责的目录；需要改契约时在交付说明中列出
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |2. 代码注释和标识符用英文；用户可见的文案同时提供 `en` 和 `zh`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |3. 通过第 2 节的检查；数据库相关逻辑必须有测试（可用 `TEST_DATABASE_URL` 时运行）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |4. 在自己的分支上提交，提交信息格式 `feat(next/<模块>): ...`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |5. 交付说明写明：完成了什么、没完成什么、对其他模块的假设、需要主控组装的内容（构造函数签名、依赖）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 11. 跨 agent 约定的格式
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.1 插件市场索引（C1 读取，E 的 `sub2api-plugin index` 生成）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |`index.json`：
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |```json
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |{
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |  "version": 1,
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |  "generated_at": "2026-09-24T12:00:00Z",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |  "plugins": [{
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    "key": "anthropic",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    "name": {"en": "Anthropic", "zh": "Anthropic"},
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    "description": {"en": "...", "zh": "..."},
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    "publisher": "sub2api",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    "versions": [{
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |      "version": "0.1.0",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |      "url": "https://example.com/anthropic-0.1.0.s2plugin",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |      "sha256": "<hex of the .s2plugin file>",
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |      "size": 1234567,
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |      "host_compat": ">=0.1.0 <0.2.0"
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |    }]
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |  }]
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |}
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |```
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |`index.json.sig`：一行 base64，内容为 Ed25519 对 ASCII 字符串 `"sub2api-market-index-v1:" + sha256hex(index.json 原始字节)` 的签名，使用市场源配置的公钥验证。`url` 可以是相对地址（相对 index.json 所在目录）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.2 插件出口 SDK（D 实现 `sdk/pluginsdk/egress`，E 调用）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |```go
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |package egress
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |// Install routes http.DefaultTransport and net.DefaultResolver through the
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |// host EgressService. Safe to call once, after InitHost.
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |func Install(client pluginv1.EgressServiceClient) error
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |// DialContext opens a tunnelled TCP connection (for DB/Redis/gRPC clients).
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |func DialContext(ctx context.Context, network, address string) (net.Conn, error)
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |```
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |插件的 PostgreSQL 连接（受限 DSN）必须使用 `DialContext`（pgx: `cfg.DialFunc = egress.DialContext`）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.3 插件主进程启动（C2 调 D）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |C2 通过 `core.PluginLauncher.Command` 得到 `*exec.Cmd` 交给 go-plugin 的 `ClientConfig.Cmd`；进程启动后调用 `Watch`。主进程的隐藏子命令 `sub2api plugin-exec` 由 D 实现 `sandbox.RunExec(args []string) int`，主控在 `main.go` 接上。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 插件二进制路径 `runtimes/{os}-{arch}/plugin`，Windows 开发模式为 `plugin.exe`；go-plugin 使用 `SkipHostEnv=true`，插件只拿到 `LaunchSpec.Env`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 无 cgroup：`LaunchSpec.CPU` 仅换算成 `GOMAXPROCS=ceil(CPU)`；`MaxThreads` 超限只告警不杀进程；`MemoryMB` 超限由看门狗重启
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 平台插件处理本平台账号时，`Account` 消息里携带解密后的凭证
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.5 缓存 token 口径（E 插件上报，G 填写 `core.UsageTokens`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |Anthropic 的 `cache_creation_input_tokens` 是总量（含 1 小时缓存）。`UsageTokens.CacheCreation = 总量 − cache_creation_1h_tokens`，`CacheCreation1h = cache_creation_1h_tokens`，两者不重叠。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.6 网关请求 ID 与插件端点状态（G）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 请求 ID 一律服务端生成（客户端的 `X-Request-Id` 只记录，不作计费幂等键）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 插件未启用（已禁用或未安装）时，它声明的网关端点不存在，请求返回 404；`plugin_unavailable` 只用于插件已启用但进程暂时不可用（按失败切换处理）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 错误格式：`plain` 即核心 REST 格式 `{"error":{code,message}}`；`anthropic` 格式在 `error` 中额外带 `code`（如钩子拒绝时的 `guard_blocked`）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 所有尝试都失败时：最后一次是上游错误则返回该错误（按 ClassifyError 的状态码与类型）；是插件或账号问题返回 503 `no_available_account`；有账号但并发槽位全满返回 429
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- `usage_logs.error_type` 取值另含 `model_not_allowed`、`price_not_configured`、`rate_limited`、`invalid_request`、`plugin_unavailable`、`blocked_by_hook`
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 钩子熔断按节点计数：同一钩子在本节点连续失败 10 次后熔断 30 秒
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 测试环境的市场地址为内网 `http://caddy:3120/market/index.json`（市场客户端目前不过滤内网地址；市场源只有 `publisher:manage` 能配置）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.7 插件调用超时（C2）与任务、事件（H）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 超时：控制台路径的平台调用（ValidateCredentials、BuildTestRequest）10 秒，请求热路径 2 秒，调度扩展点 200 毫秒，钩子按 manifest 且最多 2 秒
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 数据迁移 `MigrateData` 在协调者被接管后可能重复执行，插件必须保证幂等
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 任务 cron 默认按 UTC 计算（可用 `CRON_TZ=` 前缀指定时区）；`@every` 的触发时间对齐到周期整数倍，各节点一致；每个触发时间点全集群只执行一次
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |- 事件投递至少一次：`OnEvents` 返回的确认 id 没有超过游标时按失败处理（退避，连续 10 次失败的批次进入死信）；新订阅从当前最大事件开始，不回放历史
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |### 11.4 测试用 mock 上游（QA 实现）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |compose 里的 `mock-upstream` 服务模拟 Anthropic `/v1/messages` 与 `/v1/messages/count_tokens`：流式与非流式都返回带 usage 的响应；请求头 `x-mock-status: 429|401|529|500` 时返回对应错误（429 带 `retry-after: 2`）；`x-mock-delay-ms` 控制延迟。测试环境设置 `SUB2API_GATEWAY_ALLOW_PRIVATE_UPSTREAM=true`，账号 `base_url` 指向 `http://mock-upstream:8080`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 12. 账号类型与端点多对多、全局定价（2026-09-24，ARCHITECTURE 6.6 / 7.3）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |本节优先于前文中与之冲突的描述。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**manifest**：`accountTypes` 移到顶层（任何插件都可声明），每个账号类型有 `protocols: [{protocol, requestFields?, passHeaders?, usage?}]`，列出上游原生支持的协议；`platform` 不再含 `accountTypes`；`pricing[]` 不再有 `platform`。声明账号类型需要 capability `platform.adapter.v1` 和宿主权限 `platform.register`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**core**：`AccountTypeKey{PluginKey, Type}`；`ProtocolConverters.CanConvert(clientProtocol, upstreamProtocol)`（网关实现，账号模块用来列出经转换可服务的端点）；`AccountTypeBinding{Plugin, Type, FormSchema, FormUI, Client}`（`Client` 为声明插件）；`Generation.AccountType(pluginKey, typeID)`、`AccountTypesForProtocol(protocol)`；`AccountRef` 去掉 `Platform`；`AccountDirectory.Candidates(ctx, groupID, []AccountTypeKey)`；`Pricer.Resolve(ctx, model)`；`PriceCatalog.SyncPluginDefaults(ctx, tx, pluginKey, entries)`；`UsageRecord` 新增 `AccountType`、`UpstreamProtocol`，`Platform`/`Protocol` 为客户端端点的平台和协议，`PluginKey` 为账号类型所属插件。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**proto**：`RequestMeta.protocol` = 发给上游的协议；新增 `RequestMeta.client_protocol` = 客户端端点协议；`Account.platform` = 客户端端点所属平台，`Account.type` = 账号类型 id。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**数据库（0005）**：`accounts.platform` 删除（账号类型 = `plugin_key` + `type`）；`model_prices.platform` 删除，唯一约束改为：管理员价格每个 `model_pattern` 一条，插件默认价格每个 `(plugin_key, model_pattern)` 一条；`usage_logs` 新增 `account_type`、`upstream_protocol`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**调度**：端点协议 P → 候选账号类型 = 原生支持 P 的，加上支持 Q 且核心有 `P→Q` 转换器的（原生优先）→ 分组内这些类型的账号。请求由账号类型所属插件构造；需转换时核心转换请求体和响应（含 SSE）。用量规则：账号类型为该协议声明的 `usage`，否则取声明该协议端点的平台的 `usage`；`requestFields`、`passHeaders` 同理。错误格式始终是端点的 `errorFormat`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**计费**：`Resolve(model)` 只按模型匹配：管理员价格优先，其次插件默认价格；同一来源内模式越具体越优先（精确优先于通配、通配越长越优先），同样具体的插件默认价格取最早安装的插件；表达式结果为基础价格 × 分组倍率。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**凭证授权**：`accounts.credentials` 的范围为 `{"types": "own"}`：插件只能拿到自己声明的账号类型的账号凭证。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**REST 变更**
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 接口 | 变更 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/account-types` | 每项 `{plugin_key, plugin_name, plugin_version, asset_base, trust, type, label, description, form, sensitive_fields, protocols:[protocol], endpoints:[{method, path, protocol, platform, native}]}`；`endpoints` 为该类型当前可服务的已启用端点（`native=false` 表示经核心转换） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/account-types/:plugin_key/:type/form` | 取代 `/:platform/:type/form` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/accounts` | 筛选参数 `?plugin_key=&type=&group_id=&status=&q=`（去掉 `platform`）；每项返回 `plugin_key`、`type`、`type_label`，不再有 `platform` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || POST `/accounts` | `{name, plugin_key, type, group_ids[], proxy_id, priority, max_concurrency, schedulable, credentials}` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET/POST `/prices`、preview、validate | 去掉 `platform`（请求与响应） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 插件 `review` | 顶层 `account_types:[{id, label, protocols[]}]`；`platform` 不再含 `account_types` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 使用记录 | 列表与详情新增 `account_type`、`upstream_protocol` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 事件 `account.*` | payload `{account_id, plugin_key, type, name}`（`status_changed` 另含 `status, reason, cooldown_until?`），去掉 `platform` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**转换失败**：请求无法转换时返回 400 `invalid_argument`（记录类型 `invalid_request`，同类型其他账号跳过、不计失败切换次数）；上游响应无法转换且尚未写出内容时返回 502 `upstream_error`，上游已产生的用量照常计费。没有任何账号类型能服务该端点时返回 503 `no_available_account`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**组装**：同一个 `convert.Registry`（`convert.Default()`）交给 gateway（`Deps.Converters`）和 account（`Deps.Converters`）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**插件未启用**：端点不存在，返回 404（见 §11.6）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |## 13. 平台声明端点、账号类型声明平台、内置平台（2026-09-25，ARCHITECTURE 6.6）
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |本节优先于 §12 及前文中冲突的描述。§12 中"全局定价""凭证授权""转换失败""插件未启用 404"等仍然有效。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**概念**：平台声明端点；账号类型声明支持的平台；账号属于账号类型；分组是一组账号（可混放类型）；API Key 只绑定一个分组。核心内置平台 `anthropic`、`openai`、`gemini`（`server/internal/platforms`，嵌入的 JSON，格式同 manifest `Platform`；任何模块都可以 import，和 `core` 一样）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**manifest**：删除顶层 `gateway`、`platform`；新增 `platforms: [Platform]`（插件自己的新平台）：`{id, label, endpoints:[Endpoint], requestFields, passHeaders, usage, stickyRules}`，平台协议由端点推出（`Platform.Protocols()`）。`Endpoint` 新增 `usage`（覆盖平台用量规则）、`request.modelParam`（模型取自路径参数）、`request.stream`（端点固定为流式）；路径段可以是 `:param` 或 `:param:suffix`（如 `/v1beta/models/:model:generateContent`）。端点协议必须是 `<平台 id>.<名称>`。`AccountType.protocols` 改为 `platforms: [{platform, requestFields?, passHeaders?, usage?: {<protocol>: UsageRules}}]`。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**校验与冲突**：插件平台 id 不能是内置 id，也不能与其他已安装插件的平台 id 重复；插件平台的端点（method + 路径模式重叠）不能与内置平台或其他插件平台冲突；声明平台需要 `gateway.endpoint`、`platform.register`；声明账号类型需要 `platform.adapter.v1`、`platform.register`、`accounts.credentials {"types":"own"}`；账号类型的 `platforms` 非空，引用的平台可以是内置平台、本插件平台或其他插件平台（未启用时该类型暂时不服务它）。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**core**：`EndpointBinding{Plugin, Platform, Endpoint}`；`PlatformBinding{Plugin, Builtin, Platform}`（去掉 `Client`）；`Generation.Platforms()`、`Platform(id)`、`PlatformForProtocol(protocol)`、`AccountTypesForPlatform(platformID)`（取代 `PlatformsForProtocol`、`AccountTypesForProtocol`）；`AccountTypeBinding.Supports(platformID)`（取代 `Protocol()`）。`Generation.Endpoints()` 包含内置平台和已启用插件平台的端点。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**调度**：端点 → 平台 P、协议 X；候选账号类型 = `AccountTypesForPlatform(P)`（原生），加上支持其他平台 Q、且 Q 的某个协议 Y 满足 `CanConvert(X, Y)` 的类型；候选账号 = API Key 所属分组中这些类型的账号。requestFields / passHeaders：账号类型对该平台的覆盖 → 平台默认。用量规则：账号类型 `usage[协议]` → 端点 `usage` → 平台 `usage`（需转换时按上游协议 Y 所属平台和端点取）。内置平台的默认粘性规则写入 `sticky_rules`，`source='builtin'`、`plugin_key` 为空，只允许改启用状态和优先级。
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |**REST 变更**
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 接口 | 变更 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 ||---|---|
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/platforms`（新，`account:read`） | `[{id, label, builtin, plugin_key, endpoints:[{method, path, protocol, billing}], account_types:[{plugin_key, type, label}]}]`：内置平台 + 已启用插件的平台，以及支持它的已注册账号类型 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/account-types` | 每项新增 `platforms:[{id, label, builtin, available}]`（`available`=该平台当前存在）；去掉 `protocols`；`endpoints` 为这些平台的端点（`native=true`）加可经转换服务的端点（`native=false`），每项含 `platform` |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/groups`、`/groups/:id`、`/me/groups` | 新增 `platforms:[id]`：分组内账号的类型所支持的平台（去重、排序），即绑定该分组的 API Key 能访问的平台 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || GET `/me/api-keys`、`/api-keys` | 每项新增 `platforms:[id]`（同其分组） |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 插件 `review` | `platforms:[{id, label, endpoints:[{method, path, protocol, billing}]}]`，`account_types:[{id, label, platforms:[id], form_mode}]`；`gateway_endpoints` 由平台端点推出 |
| `server/internal/core`、`config`、`store`、`httpapi`、`testutil`、`migrations`、`secret`（AES-GCM）、`deps`、`platforms`（内置平台定义，任何模块可 import）、`cmd/sub2api/main.go`、`internal/app` | 主控 | 共享基础、组装 || 使用记录 | 不变（`platform` = 端点所属平台） |
